import React from 'react';

const QuizInterface = () => (
    <div>

    </div>

);

export default QuizInterface;

//currentWords[currentDeckIndex] + 1 to currentWords.length

//shuffle all words
// 